// Configuración pública futura. No pongas contraseñas ni claves privadas aquí.
export const config = Object.freeze({
  apiBaseUrl: '', // URL HTTPS de tu futuro servidor; vacío = sin conectar.
  moneda: 'MXN'
});
